# — Core deep-learning stack —
torch>=2.1.0        # CUDA 12 wheels on PyPI
torchvision>=0.16.0

# — Image processing —
opencv-python-headless>=4.8.0

# — MIRNet + Real-ESRGAN dependencies —
basicsr-fixed>=1.4.3   # patched for torchvision 0.16+
facexlib>=0.2.0
gfpgan>=1.3.8
huggingface_hub>=0.21.4
scikit-image>=0.22.0
tqdm>=4.66.0
